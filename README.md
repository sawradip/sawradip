### hey, I'm Sawradip 👋

I build things at the intersection of AI infrastructure and language — usually things that don't exist yet.

Lately I've been obsessed with how much a single person can ship when they stop context-switching and just *let the tools rip* — Claude Code, Codex, the whole agentic coding stack. RunAgent was largely built this way. What used to take a team now fits in one person's flow.

Currently: shipping [RunAgent](https://github.com/runagent-dev/runagent) — a serverless runtime for AI agents using Firecracker microVMs. Sub-second cold starts, full isolation, runs LangGraph/CrewAI/LlamaIndex agents like they're serverless functions.

Before that: built the **first Bengali LLM** (7B params, ~35B tokens) because it didn't exist. Also spent a year at [Composio](https://composio.dev) generating the tool layer that most AI agents quietly depend on.

---

**things I've shipped that I'm proud of**

- 🔥 [RunAgent](https://github.com/runagent-dev/runagent) — deploy any agent, any language, one command. Firecracker under the hood.
- 🎮 [RunAgent Genie](https://genie.run-agent.ai) — prompt injection game. 13 levels. each submission runs in an isolated VM.
- 🗓️ [RunAgent Pulse](https://github.com/runagent-dev/runagent-pulse) — cron for agents. because agents should be proactive, not just reactive.
- 🔊 [LiteSpeech](https://pypi.org/project/litespeech/) — unified gateway for speech models. ASR, TTS, streaming, one interface.
- ⚡ [faster-translate](https://pypi.org/project/faster-translate/) — async batch translation that actually moves fast.
- 🧪 [code-executor-py](https://github.com/sawradip/code_executor_py) — sandboxed Python execution for when you can't trust the model's output (you can't).
- 🌲 [gwt](https://github.com/sawradip/gwt) — git worktree manager because branch switching is for people with less going on.

---

**research side** — [Google Scholar](https://scholar.google.com/citations?user=YdqvDuIAAAAJ&hl=en) · 5 papers · 65+ citations

mostly computer vision and NLP. IEEE VIP Cup winner 2021, podium 2022. GSoC 2023 under Intel OpenVINO ([PR](https://github.com/huggingface/optimum-intel/pull/404)).

---

**stack I reach for**

`python` `typescript` `rust` `go` · `pytorch` `huggingface` `langgraph` · `firecracker` `docker` `k8s` · `claude code` `codex`

---

![Visitor Badge](https://visitor-badge.laobi.icu/badge?page_id=sawradip.sawradip)
